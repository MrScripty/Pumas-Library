name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  # Build and test Rust backend on all platforms
  build-rust:
    name: Build Rust (${{ matrix.target }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: macos-latest
            target: aarch64-apple-darwin
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust -> target

      - name: Install Erlang (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y erlang-dev

      - name: Install Erlang (macOS)
        if: runner.os == 'macOS'
        run: brew install erlang

      - name: Install Erlang (Windows)
        if: runner.os == 'Windows'
        run: choco install erlang -y

      - name: Set Erlang include path
        shell: bash
        run: |
          if [ "$RUNNER_OS" = "Linux" ]; then
            ERTS_DIR=$(ls -d /usr/lib/erlang/erts-*/include 2>/dev/null | head -1)
          elif [ "$RUNNER_OS" = "macOS" ]; then
            ERTS_DIR=$(ls -d "$(brew --prefix erlang)"/lib/erlang/erts-*/include 2>/dev/null | head -1)
          elif [ "$RUNNER_OS" = "Windows" ]; then
            ERTS_DIR=$(ls -d "/c/Program Files/Erlang OTP"/erts-*/include 2>/dev/null | head -1)
          fi
          if [ -n "$ERTS_DIR" ]; then
            echo "ERTS_INCLUDE_DIR=$ERTS_DIR" >> $GITHUB_ENV
            echo "Found ERTS include: $ERTS_DIR"
          else
            echo "WARNING: Could not find ERTS include directory"
          fi

      - name: Build Rust
        working-directory: ./rust
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build Rustler NIF
        working-directory: ./rust
        run: cargo build --release --target ${{ matrix.target }} -p pumas_rustler

      - name: Run Rust tests
        working-directory: ./rust
        run: cargo test --release --target ${{ matrix.target }}

      - name: Upload Rust artifact
        uses: actions/upload-artifact@v4
        with:
          name: rust-${{ matrix.target }}
          path: |
            rust/target/${{ matrix.target }}/release/libpumas_uniffi.so
            rust/target/${{ matrix.target }}/release/libpumas_uniffi.dylib
            rust/target/${{ matrix.target }}/release/pumas_uniffi.dll
            rust/target/${{ matrix.target }}/release/libpumas_rustler.so
            rust/target/${{ matrix.target }}/release/libpumas_rustler.dylib
            rust/target/${{ matrix.target }}/release/pumas_rustler.dll
          if-no-files-found: ignore

      - name: Upload pumas-rpc for Electron packaging
        uses: actions/upload-artifact@v4
        with:
          name: rpc-${{ matrix.target }}
          path: |
            rust/target/${{ matrix.target }}/release/pumas-rpc
            rust/target/${{ matrix.target }}/release/pumas-rpc.exe
          if-no-files-found: ignore

  # Build and test frontend
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Lint
        working-directory: ./frontend
        run: npx eslint src/

      - name: Type check
        working-directory: ./frontend
        run: npm run check:types

      - name: Run tests
        working-directory: ./frontend
        run: npm run test:run

      - name: Build
        working-directory: ./frontend
        run: npm run build

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/

  # Generate binding source files (platform-independent, only needs one OS)
  generate-bindings:
    name: Generate Binding Sources
    needs: [build-rust]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Download Linux Rust artifact
        uses: actions/download-artifact@v4
        with:
          name: rust-x86_64-unknown-linux-gnu
          path: rust-artifact

      - name: Package pumas-core crate
        working-directory: ./rust
        run: cargo package -p pumas-library --allow-dirty

      - name: Upload pumas-core crate
        uses: actions/upload-artifact@v4
        with:
          name: pumas-core-crate
          path: rust/target/package/pumas-library-*.crate

      - name: Install uniffi-bindgen
        run: cargo install uniffi-bindgen-cli

      - name: Install uniffi-bindgen-cs
        run: CARGO_NET_GIT_FETCH_WITH_CLI=true cargo install uniffi-bindgen-cs --git https://github.com/NordSecurity/uniffi-bindgen-cs --tag v0.9.0+v0.28.3

      - name: Generate Python bindings
        run: |
          mkdir -p bindings/python
          uniffi-bindgen generate \
            --library \
            --language python \
            --out-dir bindings/python \
            rust-artifact/libpumas_uniffi.so

      - name: Generate Kotlin bindings
        run: |
          mkdir -p bindings/kotlin
          uniffi-bindgen generate \
            --library \
            --language kotlin \
            --out-dir bindings/kotlin \
            rust-artifact/libpumas_uniffi.so

      - name: Generate Swift bindings
        run: |
          mkdir -p bindings/swift
          uniffi-bindgen generate \
            --library \
            --language swift \
            --out-dir bindings/swift \
            rust-artifact/libpumas_uniffi.so

      - name: Generate Ruby bindings
        run: |
          mkdir -p bindings/ruby
          uniffi-bindgen generate \
            --library \
            --language ruby \
            --out-dir bindings/ruby \
            rust-artifact/libpumas_uniffi.so

      - name: Generate C# bindings
        run: |
          mkdir -p bindings/csharp
          uniffi-bindgen-cs \
            --library \
            --out-dir bindings/csharp \
            rust-artifact/libpumas_uniffi.so

      - name: Upload binding sources
        uses: actions/upload-artifact@v4
        with:
          name: binding-sources
          path: bindings/

  # Package Electron app for each platform
  build-electron:
    name: Package Electron (${{ matrix.platform }})
    needs: [build-rust, build-frontend]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            rust-target: x86_64-unknown-linux-gnu
            binary-name: pumas-rpc
          - os: windows-latest
            platform: win
            rust-target: x86_64-pc-windows-msvc
            binary-name: pumas-rpc.exe
          - os: macos-latest
            platform: mac
            rust-target: aarch64-apple-darwin
            binary-name: pumas-rpc
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Download pumas-rpc binary
        uses: actions/download-artifact@v4
        with:
          name: rpc-${{ matrix.rust-target }}
          path: rust-artifact

      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist

      - name: Stage Rust binary
        run: |
          mkdir -p electron/resources/bin
          cp "rust-artifact/${{ matrix.binary-name }}" "electron/resources/bin/${{ matrix.binary-name }}"
          chmod +x "electron/resources/bin/${{ matrix.binary-name }}"
        shell: bash

      - name: Install Electron dependencies
        working-directory: ./electron
        run: npm ci

      - name: Install dmg-license (macOS only)
        if: matrix.platform == 'mac'
        working-directory: ./electron
        run: npm install dmg-license

      - name: Build Electron TypeScript
        working-directory: ./electron
        run: npm run build

      - name: Package Electron app
        working-directory: ./electron
        run: npx electron-builder --${{ matrix.platform }} --publish never

      - name: Upload Electron artifact
        uses: actions/upload-artifact@v4
        with:
          name: electron-${{ matrix.platform }}
          path: |
            electron/release/*.AppImage
            electron/release/*.deb
            electron/release/*.exe
            electron/release/*.dmg
            electron/release/*.zip
          if-no-files-found: warn

  # Create release when a tag is pushed
  release:
    name: Create Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-rust, build-electron, generate-bindings]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Rename and organize release artifacts
        run: |
          mkdir -p release-artifacts
          VERSION=${{ steps.version.outputs.version }}

          # --- Per-language binding packages ---
          # Each package contains the generated source files + native libraries for all platforms

          # --- pumas-core source crate ---
          cp artifacts/pumas-core-crate/pumas-library-*.crate release-artifacts/

          # Collect native libraries by type
          mkdir -p staging/uniffi staging/rustler

          for target_dir in artifacts/rust-*/; do
            TARGET=$(basename "$target_dir" | sed 's/^rust-//')
            mkdir -p "staging/uniffi/${TARGET}" "staging/rustler/${TARGET}"

            for file in "$target_dir"*; do
              [ -f "$file" ] || continue
              BASENAME=$(basename "$file")
              case "$BASENAME" in
                libpumas_uniffi.*|pumas_uniffi.dll)
                  cp "$file" "staging/uniffi/${TARGET}/"
                  ;;
                libpumas_rustler.*|pumas_rustler.dll)
                  cp "$file" "staging/rustler/${TARGET}/"
                  ;;
              esac
            done
          done

          # --- Per-language binding packages ---
          # Each package contains the generated source files + native libraries for all platforms
          for lang in python kotlin swift ruby csharp; do
            mkdir -p "staging/${lang}-pkg/native"
            cp -r "artifacts/binding-sources/${lang}/." "staging/${lang}-pkg/"
            cp -r staging/uniffi/* "staging/${lang}-pkg/native/"
            cd "staging/${lang}-pkg"
            zip -r "../../release-artifacts/pumas-bindings-${lang}-${VERSION}.zip" .
            cd ../..
          done

          # Elixir/Erlang NIF: precompiled native libraries
          cd staging/rustler
          zip -r "../../release-artifacts/pumas-nif-elixir-${VERSION}.zip" .
          cd ../..

          # --- Desktop application ---

          # Electron installers (already named with version by electron-builder)
          for electron_dir in artifacts/electron-*/; do
            for file in "$electron_dir"*; do
              [ -f "$file" ] || continue
              cp "$file" "release-artifacts/"
            done
          done

      - name: List release artifacts
        run: ls -la release-artifacts/

      - name: Generate checksums
        working-directory: release-artifacts
        run: sha256sum * > checksums-sha256.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-artifacts/*
          draft: true
          generate_release_notes: true
          prerelease: ${{ startsWith(github.ref_name, 'v0.') }}
