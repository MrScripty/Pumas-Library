# Artifact Load Target Fixtures

These fixtures pin the serde wire contract for selected-artifact load-target
requests and responses. They intentionally use snake_case field names and typed
diagnostic codes so consumers do not parse message text.
