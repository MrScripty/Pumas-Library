# Issues: Frontend and UI Standards Remediation

## Current State

The rows below route accepted audit findings and execution discoveries. The
[selected desktop checkpoint](execution-ledger.md#2026-09-05--selected-desktop-consumer-checkpoint-accepted)
reconciles commits `2b081fba` and `2b9553a0`; remaining milestone claims are not
closed by selected source or Linux GUI acceptance.

| ID | Severity | Audit source / evidence | Disposition | Owner / dependency | State |
| --- | --- | --- | --- | --- | --- |
| FE-I53 | medium | Runtime-profile snapshot refresh replaced the serving dialog's local draft because initialization followed selected-profile object identity, resetting an edited context such as `18000` to `4096` after the outgoing request | Initialize once per semantic model/profile/provider/mode target. Same-target snapshot clones preserve local edits across success, false and transport refresh; deliberate target changes receive current defaults. Exact context `18000` survives the tested refresh | Serving dialog draft ownership | `resolved` |
| FE-I54 | medium | A managed router started before model selection retained global context `None`; serving that model at context `18000` compared the request against the launch receipt and rejected before the load request | Keep router CLI context unset and serialize model operations through the retained same-generation worker. Write the requested context to the per-model preset, perform one reload, and require exact argument/property evidence before Loaded publication. Cancellation remains uncertain and never triggers automatic restart or context reduction | Managed llama.cpp profile-first context composition | `resolved` |
| FE-I55 | medium | A matching loading/failed serving row could project Loaded, and a successful Loaded action notice remained after a later authoritative empty/failed snapshot | Project readiness only for exact `load_state:loaded`. Tag both observed and action-success notices as Loaded-origin state so authoritative absence/failure clears them while preserving current validation/load/transport errors | Model-serving renderer readiness projection | `resolved` |
| FE-I52 | medium | Managed runtime-profile state changes are snapshot overlays without complete lifecycle push invalidation, so other mounted consumers and natural child exit can leave a prior lifecycle label visible | The accepted llama.cpp serving dialog performs one snapshot read after every serve/unserve outcome, including false and transport failure, and suppresses stale state when that refresh fails. Complete owner-driven lifecycle invalidation remains separate | Runtime-profile publication / frontend consumers | `open` |
| FE-I51 | medium | Runtime stop swallows scan/read/parse failures; counts already-dead PIDs as stopped; may return true when other attempts fail; removes PID files even after false termination; and sets cached running false on any success, while false also covers no manager/no candidates | The accepted managed-profile subset uses retained Child/group custody and terminal evidence. Legacy stop routes retain the current boolean without proving complete shutdown; reconcile target outcomes and surviving processes before authoritative absence | Backend runtime stop truthfulness | `open` |
| FE-I50 | high | Runtime stop trusts reused/unverified PID-file values including Unix zero, signals process groups/PIDs, and falls back to global command-substring scans (`ollama serve`/`serve.py`) that are not scoped to the runtime root or version | Managed binary profiles now avoid PID adoption/global scans and stop their retained cooperating process group. Legacy global stop/liveness routes remain open and require retained identity before signaling | Backend runtime stop target identity/security ownership | `open` |
| FE-I49 | medium | Runtime launch readiness observes a fixed endpoint with TCP connect only, without correlating it to the spawned PID or selected version; `ready:false` can accompany success, `try_wait` errors are ignored, and readiness does not prove continuing health | Dedicated llama.cpp requires `/health`, `/v1/models` exact-model evidence and positive owned-listener attribution. Managed routers now wait for a process/listener-bound readable catalog, submit one load request, and require the exact selected model to report loaded before guarded publication. Legacy TCP launch readiness remains unchanged and cannot establish identity or continuing health | Backend runtime launch readiness ownership | `open` |
| FE-I48 | high | Runtime launch can outlive its request through `spawn_blocking` and a detached child; stop also uses `spawn_blocking` without aggregate cancellation/deadline/per-runtime serialization. Both can leave partial effects and uncertain responses, while launch has no retained operation identity and uses an unretained observer thread | Managed Linux binary profiles now retain Child/worker/generation ownership, close admission during shutdown, cancel and clean a cooperating process group, and exclude configuration replacement while owned. The RPC `shutdown` result stops managed profiles but does not request server-process exit. Legacy global launch/stop and complete RPC shutdown remain outside this accepted subset | Backend runtime process lifecycle ownership | `open` |
| FE-I47 | high | Composed launch persists version selection before unsupported-target rejection or a failed/uncertain launch; the later launch rereads mutable active state, so concurrent selection can break the requested-version binding and no rollback exists | Preserve exact sequencing and never retry or claim rollback. Design one atomic or identity-bound selection-and-launch owner before treating composed success/failure as authoritative requested-version activation | Desktop/backend composed launch ownership | `open` |
| FE-I46 | medium | Dependency installation ignores `ensurepip` and pip-upgrade outcomes, omits constraints read/parse/get failures, and ignores pip-cache directory creation failures; a successful final install does not prove those prerequisites or constraints were applied | Preserve current response semantics in the bounded contract slice. Before using success as authoritative readiness or constraints evidence, make each required prerequisite and constraint/cache decision part of the owned terminal outcome | Backend runtime dependency-installation truthfulness | `open` |
| FE-I45 | high | Dependency installation drains stdout completely before waiting and only then drains stderr, which can deadlock on a full stderr pipe; it has no cancellation, deadline, `kill_on_drop`, per-runtime admission or progress observer through RPC, and an uncertain response can follow partial effects | Preserve no automatic renderer retry. Design retained operation identity, concurrent pipe draining, bounded cancellation/termination, serialized per-runtime admission and observable terminal reconciliation before expanding this mutation workflow | Backend runtime dependency-installation lifecycle | `open` |
| FE-I44 | medium | `get_release_dependencies` treats an absent version directory or requirements file as successful empty dependencies and uses approximate line splitting that ignores include/options, retains unsupported constructs, and differs from the dependency-check parser | Preserve the reachable wire in this response-only slice. Before using the list for authoritative completeness, readiness or automatic repair, define absence/version identity and adopt or justify a requirements parser with explicit supported syntax and error semantics | Backend runtime release-dependency ownership | `open` |
| FE-I43 | medium | `DependencyManager::get_installed_packages` treats failed `pip list`/fallback commands and unusable output as an empty installed-package list, allowing a successful dependency report to present missing data without distinguishing an authoritative empty environment from an observation failure | Keep outside the response-only contract. Add an explicit dependency-observation outcome or equivalent producer truthfulness policy before using this report for readiness, automatic repair or authoritative replacement; the generated response preserves the current wire without inventing a failure branch | Backend runtime dependency-status ownership | `open` |
| FE-I42 | high | Dependency checking, release listing, dependency installation and active-version launch derive runtime executable/PID/log or dependency paths from unbounded tags; exact string admission does not prove canonical containment before reads, subprocess execution or mutation | Keep outside these contract slices. Before expanding runtime operations, prove a canonical contained installed-runtime identity and reject traversal/absolute/separator aliases for every supported producer before effects | Backend runtime request admission/security ownership | `open` |
| FE-I41 | high | Installation accepts an unbounded exact release tag and later joins it into temporary/final runtime paths; worker reachability requires successful release resolution, but that does not establish a canonical contained filesystem identity | Keep outside the structural start-contract slice. Before changing or further exposing installation, prove a canonical contained version identity for every supported producer and reject traversal/absolute/separator aliases before filesystem effects | Backend runtime installation request admission/security ownership | `open` |
| FE-I40 | high | `install_version` can spawn a detached worker before its response is lost or rejected as malformed; the hook clears its optimistic lifecycle after any unconfirmed start and does not immediately reconcile progress, so backend work may continue without the requested-install observer | Preserve no automatic mutation retry. Design identity-bound start/observation reconciliation before treating an uncertain response as no work; the existing release `installing` hint can rediscover only separately represented work | Frontend/backend installation lifecycle ownership | `open` |
| FE-I39 | high | `VersionManager::install_version` resets shared cancellation and sets a singleton installing tag before fallible release resolution, releases its install lock after spawning rather than after worker completion, retains no worker handle and schedules identity-free delayed tracker cleanup. Resolution failure can leave a stale installing tag; overlapping workers can overwrite tag/progress and interfere through cancellation/cleanup | The accepted start contract describes admission only. Independently design atomic admission, per-operation identity, retained worker custody and cleanup ownership before changing lifecycle behavior; do not retry uncertain starts automatically | Backend runtime installation lifecycle ownership | `open` |
| FE-I38 | high | Active and default selection mutate their in-memory state before awaited persistence with no rollback, so errors can follow partial effects. Active selection additionally writes one shared launcher-root `.active-version` marker for every app manager while last-selected metadata is app-specific | The accepted switch/default response contracts preserve these semantics without claiming atomicity or runtime activation. Independently design selection persistence identity/recovery before changing producer behavior; never retry selection automatically after an uncertain error | Backend runtime-version lifecycle/persistence ownership | `open` |
| FE-I37 | high | `remove_version` accepts an unbounded tag and `version_path` joins it directly beneath the runtime versions root before recursive deletion; the legacy RPC request parser establishes only that tag is a string. The prior installed-tag membership gate rejects arbitrary unregistered tags, so filesystem reachability requires a matching unsafe installed metadata key | Keep outside the response-only slice. Before changing or further exposing runtime removal, prove a canonical contained version identity and reject traversal/absolute/separator aliases before filesystem effects | Backend runtime-version request admission/security ownership | `open` |
| FE-I36 | high | `VersionManager::remove_version` checks active state separately from deletion and performs directory deletion, metadata mutation and state refresh sequentially without rollback; later errors can follow partial effects, while refresh may normalize other legacy llama.cpp SYCL state | The accepted response contract preserves these semantics without claiming atomicity. Independently design removal concurrency and recovery before changing producer behavior; do not infer rollback or retry destructive calls from an error | Backend runtime-version lifecycle ownership | `open` |
| FE-I35 | medium | `handlers/versions/release.rs::get_version_status` swallows dependency-check errors and emits empty installed/missing lists | The dedicated `check_version_dependencies` route now preserves propagated manager/version errors at its RPC boundary, but `get_version_status` still flattens dependency-check errors into empty lists. Keep that status-route repair separate; validation does not establish dependency readiness | Backend runtime dependency-status ownership | `open` |
| FE-I34 | medium | Version handlers return empty available/installed/validation results, zero/null/empty status, null installation progress, false cancellation and `get_version_info` installed false when no version manager exists; lookup returns `None` for an unregistered runtime | Preserve behavior in response-only slices; distinguish unavailable runtime from authoritative empty/uninstalled/no-progress/no-cancellation facts in a separate capability/lookup change. Typed response validation does not prove manager availability | Backend runtime-manager ownership | `open` |
| FE-I30 | medium | User confirms Sherry is experimental and not required; current core conversion registration, generated contracts and setup paths still expose it | Deferred coherent withdrawal, not further support development. Inventory and remove advertised/registered execution surfaces with coordinated contracts/tests when scheduled; preserve existing model files and caller-owned environments. No withdrawal or real-tool support claim yet; see [scope decision](plan.md#conversion-scope-and-priority) | Backend/platform/frontend; revisit before conversion is re-advertised or expanded | `open` |
| FE-I29 | medium | NINVALID's initial parallel run failed the existing safetensors preflight boolean readiness assertion; isolated and six diagnostic parallel runs passed without capturing the cause | Fixture writer now excludes a known inherited writable-descriptor hazard, and the assertion uses public fallible readiness to preserve future errors. This does not establish the original cause. Diagnose on recurrence or the next conversion-verification slice; preserve logs in the [NINVALID ledger](execution-ledger.md#2026-09-08--native-setup-interruption-invalidation), without production retries, deadline widening or serialization | Backend conversion verification | `open` |
| FE-I28 | medium | EXCL's one-second admitted-ID timeout recurred within 18 NINVALID download deadline failures; subsequent default/group runs passed | Bounded [reproduction](execution-ledger.md#2026-09-08--download-timeout-reproduction) did not establish a cause. [Test-only admission diagnostics](execution-ledger.md#2026-09-08--download-admission-wait-diagnostics) now preserve last-observed milestone and worker completion for the two admitted-ID waits. Diagnose on recurrence using that context and exact build/permissions; other effect waits remain uninstrumented. Do not repeat green suites without new deciding evidence, widen deadlines, serialize tests or claim a rerun fixes the cause. Original evidence: [EXCL](execution-ledger.md#2026-09-08--managed-setup-and-conversion-exclusion), [NINVALID](execution-ledger.md#2026-09-08--native-setup-interruption-invalidation) | Backend download verification | `open` |
| FE-I01 | high | F-01 undecoded IPC values | Selected catalog/FTS/download/ticket checkpoint `2b081fba` and subsequent HF, metadata, notes/settings and runtime slices are accepted in the [ledger](execution-ledger.md). Latest: the profile-first llama.cpp router context and truthful Loaded-state checkpoint; next: `is_ollama_running`/`is_torch_running`. Remaining consumer dispositions and generated handoffs are required before whole FE-A1 acceptance | Frontend consumer; Rust and platform providers | `open` |
| FE-I31 | high | `handlers/models/inference.rs::update_inference_settings` converted malformed settings into an empty replacement | [Strict mutation admission](plan.md#inference-settings-mutation-admission) now rejects missing/null/malformed settings and ambiguous aliases before persistence. Actual RPC regressions prove unchanged metadata for rejection, valid replacement and explicit empty clearing. Response contract remains separate | RPC request admission; backend/frontend consumers | `closed` |
| FE-I32 | medium | Metadata handler captures primary path before effective-metadata reconciliation; the unindexed tiny-file fixture was relocated and embedded extraction became absent | Response validation does not change this ordering or extraction-error omission. Investigate representative reconciliation/relocation cases before claiming metadata absence is authoritative; the accepted RPC fixture explicitly indexes its current snapshot | Backend metadata read/reconciliation | `open` |
| FE-I33 | high | `handlers/models/inference.rs::update_model_notes` converted supplied non-strings into clears | [Typed notes admission](plan.md#model-notes-mutation-admission) rejects malformed values and ambiguous aliases before writes. Real RPC tests prove unchanged metadata on rejection, exact text persistence and supported omitted/null/blank clears | RPC request admission; backend/frontend consumers | `closed` |
| FE-I02 | medium | F-02 cached model provenance | Root-scoped display-only version-2 cache and cold/warm Linux behavior accepted; complete representative degradation/retry and age evidence before whole FE-A2 acceptance | Frontend | `open` |
| FE-I03 | high | F-03 installation polling lifecycle; [async-owner inventory](reports/frontend-async-owner-inventory.md); PRG-I12 found requested-install polling could precede backend admission | Accepted after M3-S3a added and proved the deferred-admission repair | Frontend | `resolved` |
| FE-I04 | high | F-04 dialog/popup interaction; [accepted Chromium evidence](execution-ledger.md#2026-09-03--m2-s4-representative-chromium-evidence-accepted) | Accepted in Milestone 2 with focused Module/consumer tests and representative Chromium AX/focus/input evidence | Frontend | `resolved` |
| FE-I05 | medium | F-05 progress/motion semantics | Accepted in Milestone 3 with focused and representative Chromium evidence | Frontend | `resolved` |
| FE-I06 | medium | F-06 error gate | Transferred; do not duplicate here | [Governance and verification plan](../governance-and-verification/plan.md) | `transferred` |
| FE-I07 | high | F-07 frontend variants | Renderer behavior in Milestones 1/5; packaged artifacts transferred | Frontend / platform | `open` |
| FE-I08 | medium | F-08 count-based authority | Transferred; do not duplicate here | [Governance and verification plan](../governance-and-verification/plan.md) | `transferred` |
| FE-I09 | medium | [Async-owner inventory](reports/frontend-async-owner-inventory.md#unrelated-timer-dispositions) found five unrelated async `setInterval` owners without serialization or stale-completion guards | Preserve current plan scope; schedule a focused async-owner remediation after this program unless an owning plan absorbs each row | Frontend follow-up | `open` |
| FE-I10 | high | EC-18 in the governance [error-contract disposition](../governance-and-verification/reports/error-contract-gate-disposition.md): link-health refresh rejection hides unavailable state or leaves an unmarked stale projection | Public core read, validated RPC/generated preload contract, unavailable/retry UI and stale-completion exclusion accepted in the [2026-09-06 ledger](execution-ledger.md#2026-09-06--standalone-backend-and-link-health-contract) | Frontend; Rust/platform handoff accepted | `resolved` |
| FE-I11 | medium | EC-03 in the governance [error-contract disposition](../governance-and-verification/reports/error-contract-gate-disposition.md): native model-import picker rejection closes the interaction with no user-visible outcome | Closed selection/cancellation/invalid/unavailable contract, exact path preservation, visible retry and late-result exclusion accepted in the [2026-09-06 ledger](execution-ledger.md#2026-09-06--import-picker-contract-accepted) | Frontend; native picker/provider handoff accepted | `resolved` |
| FE-I12 | low | [Overlay inventory](reports/frontend-overlay-consumer-inventory.md#searched-non-members) found that the in-flow Link Health and Migration Reports disclosure triggers do not expose expanded/content relationships | Keep outside M2 because these disclosures do not share popup focus/dismiss lifecycle; schedule a focused disclosure-semantics repair | Frontend follow-up | `open` |
| FE-I13 | medium | [M3-S3 runtime admission](execution-ledger.md#2026-09-03--m3-s3-runtime-admission-stopped-for-unreachable-status-view) found no production transition into `ProgressDetailsView`; successful terminal progress and the dialog presentation tag are cleared before the accepted status semantics can be observed | M3-S3a source repair and M3-S3b representative status evidence are accepted | Frontend | `resolved` |
| PRG-I12 | high | Integration review found a requested install could read progress before backend admission, consume a prior terminal payload, invalidate its generation, and strand the newly accepted lifecycle | M3-S3a proves no read before successful admission and a current first read afterward; existing-install discovery remains immediate | Frontend / program review | `resolved` |
| FE-I14 | high | Rust M2F handoff: canonical conversion-progress core fields serialize camelCase while handwritten `frontend/src/types/api-conversion.ts` claims snake_case | Generated read/list response types and preload decoding accepted with all current enum vocabulary, explicit nulls, numeric bounds and producer-to-renderer evidence in the [2026-09-06 ledger](execution-ledger.md#2026-09-06--conversion-progress-contract-accepted) | Frontend consumer; Rust/platform handoff accepted | `resolved` |
| FE-I23 | medium | Basic GGUF/safetensors format workflow now has explicit setup consent, start/progress/cancel, reopen and completion refresh; quantization-specific GUI configuration remains absent | Accept only the bounded format workflow. Further quantization controls are deferred by the [user priority decision](plan.md#conversion-scope-and-priority), not a blocker for unrelated M4 work; if resumed, they depend on FE-I26 backend preflight and execution custody; FE-I25 base setup is accepted but does not cover native quantization installation | Frontend; backend prerequisite FE-I26 | `open` |
| FE-I26 | high | Quantization review found non-atomic admission, unobserved cancellation/shutdown, shared staging, discarded output identity, one-pipe/quiet-child stalls, installers bypassing SetupOwner, path-only readiness and unvalidated target/calibration/hardware | Manager admission, retained workers/shutdown, publication, bounded process cleanup and managed target/calibration checks are accepted in the ledger. [Installer custody](execution-ledger.md#2026-09-08--quantization-installer-custody) now retains all built-in setup work and closes all owners before aggregate drain. [Setup import verification/repair](execution-ledger.md#2026-09-08--quantization-setup-import-verification) is accepted without recreating existing environments. [Direction-specific llama.cpp artifacts](execution-ledger.md#2026-09-08--direction-specific-llamacpp-artifacts) are checked before staging/spawn. [Public quantization import readiness](execution-ledger.md#2026-09-08--retained-quantization-readiness-probes) now retains async probes, refreshes later reads and drains them at shutdown. [Native artifact verification/guarded repair](execution-ledger.md#2026-09-08--native-llamacpp-setup-artifacts) is accepted. [Backend-specific Rust setup observation/retry](execution-ledger.md#2026-09-08--backend-specific-rust-setup-observation) is accepted; verification also corrected ESRCH disappearance classification in the shared procfs stat reader without weakening custody. [RPC/desktop setup observation](execution-ledger.md#2026-09-08--backend-setup-rpc-and-desktop-projection) is accepted with generated request/response validation and controlled dispatch/preload evidence. [Managed setup/conversion exclusion](execution-ledger.md#2026-09-08--managed-setup-and-conversion-exclusion) is accepted through retained cleanup/publication/indexing at a stable root. [Retained base-format readiness](execution-ledger.md#2026-09-08--retained-base-format-readiness) is accepted, including explicit probe errors and aggregate shutdown. [Native setup source/build coherence](execution-ledger.md#2026-09-08--native-setup-source-and-build-coherence) is accepted for the successful-setup path. [Native setup interruption invalidation](execution-ledger.md#2026-09-08--native-setup-interruption-invalidation) is accepted for process-exit/reopen and explicit repair. [Calibration-file preflight](execution-ledger.md#2026-09-08--calibration-file-preflight) now shares nonempty regular-file classification and bounded open/read checks between managed admission and direct llama.cpp execution. [Direct target validation](execution-ledger.md#2026-09-08--direct-quantization-target-validation) now shares exact catalog admission across all built-in entry points. [Direct importance-matrix option validation](execution-ledger.md#2026-09-08--direct-importance-matrix-option-validation) now rejects forced matrices outside llama.cpp before effects. [Source-file discovery](execution-ledger.md#2026-09-08--quantization-source-file-discovery) now excludes nonregular candidates, retains inspection errors and selects llama.cpp input before staging; NVFP4/Sherry root prechecks remain unresolved. Further work is deferred by the [user priority decision](plan.md#conversion-scope-and-priority); it does not block unrelated M4 contracts. Remaining: later external mutation provenance, independent probe/direct/external-use coordination, tools/hardware, calibration content quality/full-readability/custody, remaining direct backend/source-file preconditions, stronger containment and safe retained-staging cleanup. Base Python input admission is outside the quantization claim. No new GUI mutations until prerequisites are accepted | Backend prerequisite to FE-I23 | `open` |
| FE-I27 | high | Script complete/error previously projected terminal status before cleanup and indexing; NVFP4/Sherry stdout JSON was log-only | Terminal authority and shared Python script projection accepted in the [2026-09-08 ledger](execution-ledger.md#2026-09-08--python-quantization-progress): retained worker receipts own completion; phases and completed-epoch fractions are observable; script diagnostics fail after cleanup even on exit zero. Controlled-process/backend fixtures prove no early publication. Real GPU/model execution and new quantization GUI remain outside acceptance | Backend progress owner / frontend consumer | `resolved` |
| FE-I25 | high | The blocking dialog setup call could time out while backend installation continued | Optional dialog now starts/observes retained setup, requires explicit identity-bound retry, reconciles uncertainty through reads, and permits active close/reopen. Accepted hook and built Linux GUI evidence is bounded in the [ledger](execution-ledger.md#2026-09-07--conversion-setup-dialog); native installation and other OS execution are not claimed | Frontend consumer; backend/bridge handoff accepted | `resolved` |
| FE-I24 | medium | Conversion operation outcomes were handwritten; quant options omitted backend/imatrix metadata and used incorrect casing | Backend-owned schemas, generated preload/renderer contracts, complete start options and readiness/setup routes accepted in the [2026-09-06 ledger](execution-ledger.md#2026-09-06--conversion-operation-contracts-accepted); actual GUI workflow remains FE-I23 | Rust/frontend; handoff accepted | `resolved` |
| FE-I15 | medium | [M3-S3b real Chromium counterevidence](execution-ledger.md#2026-09-03--m3-s3b-runtime-stopped-for-visible-reduced-motion-displacement): reduced-mode Popover entry retains `translateY(-6px)` into nonzero-opacity frames before snapping to rest; exit uses the same translated target | Accepted after exact source/test repair and four non-vacuous entry/dismiss real scenarios | Frontend / program review | `resolved` |
| FE-I16 | low | Root full-suite run passed 470/471; unchanged `ModelMetadataModal.test.tsx` queried the deleted accessible backdrop label and did not await the modal's initial async state | Accepted after correction against the ModalDialog backdrop/document Interface; focused and full suites pass without act warnings | Frontend | `resolved` |
| FE-I17 | high | Platform launcher-root recovery contract was accepted, but the renderer mounted backend-consuming content without observing startup authority and handled selection through a legacy optional-field response | Composition-root startup/selection owner and synchronous decoded bootstrap accepted in `2b081fba`; complete XR-S1 state and two-entry matrix remains pending | Frontend consumer; accepted platform producer | `open` |
| FE-I18 | high | The first XR-S1 real Electron run found the compiled sandboxed preload imported a local CommonJS decoder module and therefore failed before exposing `window.electronAPI` | Platform moved runtime decoding solely into the preload artifact, retained producer policy in its helper, and proved the corrected artifact in real OS-sandboxed Electron; the subsequent two-entry consumer oracle has no preload/console failure | Platform producer; frontend real-entry corroboration | `resolved` |
| FE-I19 | medium | Historical normal-ready run painted Checking; renderer-only timing repairs were rejected | Main-owned marker barrier and its approximately one-second cost are approved and committed; Linux cold/warm model-list presentation passes. Remaining full first-visible matrix belongs to XR-S1, not another renderer timing repair | Frontend / platform visibility seam | `open` |
| FE-I21 | high | Electron identity with an absent preload bridge was treated as ordinary browser mode and mounted valid-looking backend content | The provider now fails closed by Electron user-agent identity, renders a focused path-free unavailable view, hides unsupported Minimize, and retains working Close; this frontend subcondition is resolved while global PRG-I21 remains open through composed handshake review | Frontend | `resolved` |
| FE-I22 | medium | Current Standards rejected noncanonical prior plan, objective, ledger, report, and issue lifecycle projections | XR-S1 uses canonical `Verifying`, FE-A8 remains `pending`, unresolved issues are `open`, and the external plan-structure gate at fixed Standards `17f418fb` passes; global PRG-I22 remains program-owned | Frontend plan records | `resolved` |
| FE-I20 | high | Async renderer-only terminal timing cannot guarantee the first visible frame | Accepted marker-barrier source replaces renderer timing authority; retain the full delayed-ready/watchdog/recovery/no-preload and two-entry proof obligation before FE-A8 acceptance | Frontend / platform visibility seam | `open` |
