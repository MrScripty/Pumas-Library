# Issues: Local Intent API

| ID | Severity | Finding/evidence | Owner | Disposition |
| --- | --- | --- | --- | --- |
| I1 | Contract | `api/models.rs::get_model` already means indexed lookup | Core intent interface | Resolved in plan: separate `api.intent()` namespace; preserve existing method |
| I2 | Lifecycle prerequisite | Existing remediation plan records unfinished download restore/import/crash evidence | Existing remediation program; intent integration owner | M2 bounded ordinary resume, integrity, caller-drop and shutdown evidence accepted in [report](reports/managed-intent-acquisition.md). M3b adds bounded declaration/admission/publication process-exit evidence in its [report](reports/m3b-local-desired-state.md). Broader historical lifecycle/power-loss guarantees remain open; existing lifecycle owner retains authority |
| I3 | Persistence | Durable declaration schema and lifecycle integration need separate guarantees | Core intent/store implementation | M3a private store accepted with [implementation evidence](reports/m3a-declaration-store.md) and [exact design](reports/m3-store-design.md). M3b integration accepted: guarded deletion/relocation, owned drain, immutable replay, and startup/bounded retry; see [evidence and explicit compatibility limits](reports/m3b-local-desired-state.md) |
| I4 | Consumer scope | Existing bindings and desktop consumers use operational contracts | Local transport implementation | Existing native, UniFFI, RPC, Electron and frontend consumers inventoried in the [M4 report](reports/m4-local-transports.md). Existing operational contracts remain; new binding/UI intent projections require a separate bounded scope update |
| I5 | Deferred scope | Brief includes nodes, fleet management, remote discovery and transfers | User release scope | Deferred until after next release; revisit then, with fresh scope and acceptance. No placeholders in current implementation |
| I6 | Acquisition contract | Download requests, metadata, worker URLs, identity and ticket recovery previously assumed main; see [audit](reports/acquisition-prerequisites.md) | Existing download/import/store owners | M2a accepted: private pinning, identity, persistence/resume and import verified; ticket recovery refuses pins. See [evidence](reports/immutable-revision-implementation.md). M2 now exposes bounded native AllowUpstream after its [separate gates](reports/managed-intent-acquisition.md) passed |
| I7 | Existing test baseline | Baseline api_tests: router/other missing-binary diagnostics changed in existing process owner; runtime shutdown test also expects profiles_processed >= 1; panics poison shared registry mutex | Existing runtime-profile test owner | Astra reproduced router failure before intent changes; retain separate relevant model/library API regression evidence and disclose full-suite failure |
| I8 | Existing artifact contract | `package_facts/artifact.rs` classifies a Safetensors package with config.json as HfCompatibleDirectory, but descriptor entry_path can be its primary file while load target requires a directory | Existing package-facts/load-target owner | Open, separate canonical contract correction; Local observation retains shape checks and returns Incomplete. M2 rejects known affected upstream Safetensors layouts before transfer; no fabricated path or claim for all layouts |
| I9 | M1 review | Cached revision/repository could disagree with current metadata; unknown format and invalid GGUF evidence could be misclassified | Intent and canonical freshness helper | Resolved: current source coherence gates positive/negative decisions; unknown/invalid facts stay incomplete; header quantization matches canonical labels while filename guesses cannot satisfy quantization. Regression tests pass |
