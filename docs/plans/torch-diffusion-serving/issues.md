# Issues

| ID | Severity | Evidence / objective impact | Owner / boundary | Disposition | Verification / revisit |
| --- | --- | --- | --- | --- | --- |
| T1 | High | An upstream PyTorch release tag alone does not identify a complete serving recipe | App manager / installation | Resolved for `v2.9.1` with a separate recipe identity and embedded sidecar; packaged discovery, install, migration, activation, removal, cancellation preservation, and restart acceptance passed | [Packaged upstream acceptance](reports/a1-packaged-acceptance.md); an upgrade between two different PyTorch upstream tags awaits a second qualified recipe |
| T2 | High | Torch and torchvision must resolve to the exact qualified official wheels, not source archives or mutable package choices | Runtime packaging | Implemented with direct official wheel URLs and SHA-256 pins for the Linux x86_64 / CPython 3.12 recipe | Artifact hashes and GPU startup; re-plan if unavailable |
| T3 | High | Standalone checkpoint availability does not establish all matching pipeline assets | Library and Torch adapters | Resolved: both complete pipelines generate; encoder converted via Pumas | Exact component identities and successful real pipeline loads |
| T4 | High | Tuldok's existing local-model client sends VLM chat requests, not image generation | Tuldok / HTTP and browser | Fix in M3 | Real prompt, displayed image and saved decoded content |
| T5 | Medium | 24 GB VRAM is shared with other inference; full pipeline fit and latency require exact-candidate evidence | Serving lifecycle | Resolved: exact-candidate inference and cancellation/reuse passed; a controlled CUDA allocator OOM returned a sanitized 507 and a later real generation succeeded | [GPU OOM recovery](reports/a5-gpu-oom-recovery.md), [Nunchaku GPU evidence](reports/nunchaku.md), and TIPC-04 |
| T6 | Low | LLaDA and other platforms are not qualified for this implementation | Runtime adapter owner | Defer | Revisit after primary flow acceptance and user prioritization |
| T7 | High | The manager pointed Torch discovery at the Pumas repository and filtered out official upstream releases; the globe must show installable supported PyTorch tags | Runtime distribution | Fixed and packaged acceptance passed: discovery targets `pytorch/pytorch`, and only qualified v2.9.1 is installable. No Torch release or tag is published by Pumas. | Official release fixture and [packaged upstream acceptance](reports/a1-packaged-acceptance.md); broader upstream-to-upstream updates await another qualified recipe |
| T8 | Medium | Shared install mutex previously ended when the background task was spawned; release failure could leave a phantom installing tag | App manager lifecycle | Fixed in M1 | App-manager regression suite |
| T9 | High | Torch activation previously wrote llama.cpp's `.active-version` marker | App manager selection | Fixed in M1 with a Torch-specific marker inside shared state management | Marker-preservation regression |
| T10 | High | Real Nunchaku wheel import requires libcudart.so.13; CUDA 12.8 candidate rejected before publication | Runtime recipe | Resolved with official Torch 2.9.1 CUDA 13.0; real imports, CUDA execution and protocol-1 health passed | Real imports, CUDA operation and sidecar health |
| T11 | Medium | Full base-pipeline acquisition is 30.64 GiB and current transfer is slow | Library acquisition | Completed through Pumas authority; real Nunchaku and FLUX images passed | Complete validated library bundle before model loading |

| T12 | High | Nunchaku 1.2 positional forward arguments bind to Diffusers 0.37 ControlNet fields; native FP4 forward raises TypeError | Torch adapter compatibility | Fixed by a narrow subclass preserving rotary hooks and naming Diffusers arguments; native kernel pass and focused tests passed | Resolved: complete Nunchaku pipeline and real Tuldok workflow passed |
| T13 | High | Real Serve on an already-running managed Torch profile tried to launch it again; newly launched profiles also lacked a health wait | Torch serving lifecycle | Reuse the current owned process observation; wait for owned listener and health before load | Resolved: actual release Serve and Tuldok image flow passed |

| T14 | Medium | Standalone VAE download resolves to a package directory | Torch serving asset resolution | Fixed: resolve the known VAE file within the validated package | Final release FLUX/Tuldok FP8 image passed |
| T15 | Medium | Conversion metadata omitted indexed source names and labeled non-GGUF output as GGUF | Existing conversion pipeline | Fixed: hydrate column-owned identity, label the actual format, retain source classification | Real FP8 conversion, visible final encoder names, final release build |

| T16 | Medium | Restarting the desktop during active NVFP4 setup left the previous pip process orphaned (PPid 1, own process group) | Existing Electron/backend shutdown boundary | Operationally stopped the verified task-owned orphan before either installer wrote packages; current setup continues. Broader shutdown fix deferred from conversion-format scope | Revisit desktop shutdown while dependency setup is active; root setup unit evidence alone did not cover this actual desktop path |
| T17 | High | Prospective image deadline instructions conflict with the generation-wide no-elapsed-deadline contract | Torch Image Provider Contract Correction plus this plan's Tuldok/candidate handoffs | Resolved: the focused plan owns shared generation lifetime and the Pumas/Torch image compatibility/result/projection correction; Tuldok removed its remaining deadline and TIPC-04/TIPC-09/TIPC-10 passed | Exact installation, real GPU cancellation/reuse, and 1280×720 browser evidence passed; historical reports retain their original scope |
| T18 | High | Original M3 was marked completed before the newly delegated Tuldok deadline removal and exact-candidate 1280×720 evidence existed | M3-TIPC companion | Resolved without relabeling original evidence: `M3-TIPC` completed independently at Tuldok `a61daeec` | 47 focused/full Tuldok tests plus exact-candidate 1280×720 browser display/save passed |
| T19 | High | Production installer rejected the protocol-3 recipe's required `capabilities` field before dependency setup | App-manager immutable recipe qualification | Fixed in Pumas `ab3a95a9`: decode additive capabilities and require `image_generation`; candidate bytes unchanged | Focused red/green regression, 107 app-manager tests, and exact `0.1.6` production installation/native validation passed |
| T20 | Medium | Natural exit of a managed TorchServe child completed process-group cleanup but left its served-model row visible because terminal invalidation only ran through the llama.cpp router observer | Managed runtime lifecycle / serving state | Fixed on `work/torch-runtime-a1-a5`: retain and join terminal observers for both non-router and router profiles; invalidate only after confirmed cleanup through the current-generation fence; surface update failures as failed drains | CPU child-process regression checks row removal, `ModelUnloaded` update, and preservation of another profile's row; registry-poison tests verify both observer variants report failed generation lookup and drain state becomes failed; replacement-generation rejection remains covered |
| T21 | High | The private image route applied admission error mappings to admitted adapter execution, so adapter `KeyError`, `ValueError`, and busy-text `RuntimeError` could be exposed as 503, 400, and 409 | Torch image-provider error boundary | Fixed on `work/torch-runtime-a1-a5`: scope 503/400/409 mapping to lease admission; map admitted adapter failures to sanitized 502 while preserving OOM 507 | Registered-route regressions cover all three ambiguous adapter errors, true admission errors, immediate and delayed synthetic OOM, device-lock custody, and subsequent reuse; image boundary module passed 8 tests |
| T22 | High | Concurrent active/default selection could target an installed runtime after removal checked it but before filesystem and cached-state cleanup completed | Shared `VersionManager` lifecycle | Fixed with a lifecycle mutex serializing active/default selection and removal while keeping selection independent of long-running installs | Deterministic Torch tests cover removal-first active/default races, sequential active-selection-first refusal, default-first removal, metadata, markers, files, and reconstructed state; app-manager and Rust checks |
