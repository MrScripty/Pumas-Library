# Current Standards Remediation Issues

## Decision Dependencies

| ID | Canonical owner | Program effect | Accepted disposition | State |
| --- | --- | --- | --- | --- |
| `RUST-I1` | [Rust issues](rust-library-and-rpc/issues.md) | Unblocks remote-exposure portion of PRG-A1/PRG-A2 | Remove `--allow-lan`; reject every non-loopback `--host`; prove real loopback listener/hostile-client behavior. | `resolved` |
| `RUST-I2` | [Rust issues](rust-library-and-rpc/issues.md) | Unblocks plugin portion of PRG-A4 | Compiled-out is disabled/unavailable; configured compiled-in subsystem initialization failure fails startup without temporary-root substitution. Preserve typed optional per-plugin unavailable outcomes only when existing source proves that post-start distinction. | `resolved` |
| `RUST-I3` | Platform Milestone 0 and [Rust issues](rust-library-and-rpc/issues.md) | Unblocks binding disposition for PRG-A6 | Remove unconsumed Rustler support/source surfaces with the platform/Rust owner split. | `resolved` |
| `PLAT-I1` | [Platform Milestone 0 decision report](desktop-release-bindings-and-torch/reports/release-and-host-contract-decision.md) | Unblocks support-contract implementation for PRG-A3/PRG-A4/PRG-A6 | Preview-only three desktop tuples behind real evidence; remove `.crate` and unconsumed UniFFI/Rustler/Go outputs/surfaces; Torch non-shipped until proved; repository maintainer owns promotion and notice acceptance. | `resolved` |

New issues belong first with their focused owner and are promoted here only
when they change program sequence, shared ownership, objective scope, or
acceptance. Current program-level coordination findings follow.

## Coordination Findings

| ID | Severity | Evidence | Program relationship and owner | Disposition | Verification / revisit trigger |
| --- | --- | --- | --- | --- | --- |
| `PRG-I24` | Critical | RUST-I8's move-before-admission defect is superseded by the accepted current-only cutover; old partial relocation is removed. Core ticket/cancellation composition and selected desktop consumer migration are verified. | Former legacy composition and integration holds resolved; full C3 remains active. | Preserve exact pending custody and publication, with no guessed rollback or old-format support. Selected desktop handoff is accepted at `2b081fba`, not blocked by removed repo/path callers. | Focused Rust and parent ledgers own exact gates and remaining C3 limits. Revisit on changed custody or new transport scope. |
| `PRG-I1` | High | All four focused implementation owners started concurrently while the program requires Critical RPC disclosure containment to be the first source-code slice. | Program sequencing; program coordinator owns admission and each focused owner owns compliance. | Resolved: Rust Milestone 1 remained the sole source mutation until its focused owner accepted RUST-A1; root then admitted the three named non-overlapping streams. | Reconfirm source ownership and next integration slice at each focused handoff. |
| `PRG-I2` | High | The Rust focused plan initially projected the started lifecycle as `In Progress`, which is not a Planning lifecycle state. | Plan lifecycle Interface; Rust focused owner. | Resolved: the focused plan and milestone use `Active`; no source edit preceded the correction. | Reconfirm focused plan structure before accepting the disclosure slice. |
| `PRG-I3` | High | Governance changes overlapped CI, hooks, package manifests, contributor guidance, development documentation, and frontend command documentation; later platform/frontend work may reach some of those authorities. | Shared-write serialization; root integration owner and focused owner requesting each handoff. | Resolved for completed intervals: governance released its files; platform M0 released both matrices/`.gitignore`; platform M4 released root `README.md`/`docs/DEVELOPMENT.md`; frontend M3 released `frontend/README.md`. No owner has root package authority. | Require a new exact serialized handoff before any later package/CI/generated/current-documentation write. |
| `PRG-I4` | Medium | The first frontend inventory report was created at repository-root `reports/` although Planning assigns reports to the active plan directory. | Plan artifact ownership; frontend focused owner. | Resolved: the report and links now use the frontend plan's `reports/` directory, and the root duplicate was removed. | Re-run focused plan structure and link checks before the slice is accepted. |
| `PRG-I5` | Medium | Governance's M2 classification found that link-health refresh can silently retain or hide state and model-import picker rejection has no user-visible outcome; neither currently has a frontend milestone disposition. | Frontend projection/interaction ownership; both gaps are in this program but do not justify retaining the proxy checker. | Re-plan gate: before frontend M4, classify both consumers against its decoded bridge/projection authority. Add their exact write sets and evidence to M4 when they share that authority; otherwise create a separate focused frontend slice with behavior evidence. Active M0 absorbs only installation-cancellation feedback. | Frontend M4 consumer inventory or any earlier edit to either owner. |
| `PRG-I6` | Medium | Governance M2's shared frontend precommit was blocked by a non-null assertion lint failure in concurrent frontend M0 source at `useInstallationManager.test.ts:131`; governance's own deletion/reference/static checks and the 446-test suite passed. | Cross-plan verification dependency; frontend owned the failing source and governance owned the blocked acceptance rerun. | Resolved: frontend replaced the assertion with a typed fixture and passed focused lint/type/tests; governance reran the shared precommit and 446-test suite, then accepted M2. | Reconfirm cross-plan verification ownership at the next shared gate. |
| `PRG-I7` | High | Governance M4's first aggregate passed Rust fmt/check/Clippy but workspace tests reported two independent failures plus nine poison cascades. Rust could not reproduce either root across isolated, serial/default/128-thread, dual full-library, or 12-process API-creation stress. | Cross-plan verification dependency; Rust owned classification and governance owned final aggregate-gate reconciliation. | Resolved without speculative source change: the complete isolated aggregate passed with required local IPC permission, while restricted-sandbox reruns reproduced environment denial at varying IPC tests. Governance accepted GOV-A5/M4. | Revisit only if the same independent root reproduces in the declared capable environment. |
| `PRG-I8` | High | Frontend M2-S3 review found separate `ModalDialog` and `Popover` global stacks/document-capture Escape listeners. In a Popover nested inside a Modal, the older Modal listener could stop propagation and close the parent before the topmost Popover handled Escape. | Frontend interaction composition; contradicted the admitted topmost/nested lifecycle and blocked FE-A4/M2-S3 acceptance. | Resolved: one private `OverlayEscapeStack` Module now owns cross-overlay Escape arbitration while each overlay retains its local focus and outside-interaction lifecycle. The focused owner captured the composed failure, made it green, and root integration review accepted the revised composition. | Passed: M2-S4 observed topmost two-stage Escape/restoration, native outside-click focus ordering, listener cleanup, modal containment, and popup relationships in Electron 39/Chromium 142. |
| `PRG-I9` | Medium | Frontend M3-S1's caller suite found `InstallDialog.test.tsx` still querying the removed `Dismiss install dialog` control even though the accepted M2 Interface uses backdrop and document Escape dismissal. | Focused evidence reconciliation; frontend owns the stale test, and the mismatch blocked M3-S1's caller-level green boundary but did not invalidate the accepted real-Chromium FE-A4 behavior. | Resolved: the exact test-only M2 follow-up now exercises the current backdrop/document-Escape Interface; no product source changed, and root accepted M3-S1. | The combined ProgressDetailsView, InstallDialogContent, and InstallDialog suite passed 10 tests across three files with focused ESLint, full types, and diff checking. |
| `PRG-I10` | High | M3-S3 runtime admission found no production transition to `ProgressDetailsView`; further owner-chain inspection found successful terminal progress and its presentation tag were also cleared before observation. | Frontend product-path reachability and FE-A5 evidence; component-only tests manufactured an unreachable branch, so fixture-only Chromium evidence could not establish the user workflow or justify README claims. | Resolved: the accepted manager/dialog/state repair retains and presents active/terminal state, and M3-S3b reached it through the real default entry without test-only state injection. | Passed: real Chromium exposed two named determinate progressbars and exactly one polite atomic terminal success with its retained tag; FE-A5/M3 accepted. |
| `PRG-I11` | High | Platform M0 wrote `bindings/support-matrix.json`, but `.gitignore:96` ignored `bindings/*`; JSON parsing passed while Git status/diff exposed only the artifact plan. | Canonical support authority and shared-write tracking; an ignored matrix could not enter the reviewed change set or govern later deletion/generation. | Resolved: a narrow tracked exception admits only `bindings/support-matrix.json`; program cross-review accepted its reciprocal relationship with the artifact plan, closed identities, support dispositions, and complete acceptance prerequisites. M0 accepted and released all three shared files. | Passed: both matrices are visible to Git, parse, cross-reference, and close 3 targets, 5 artifacts, 7 removed host surfaces, 0 accepted host tuples, and all DRBT-A1–A9/PRG-A1–A7 gates. |
| `PRG-I12` | High | Root M3-S3a review found `useInstallationManager` started polling before `install_version` acknowledged the new lifecycle. A prior terminal payload could invalidate the new generation and strand the subsequently accepted install without polling. | Frontend admission/lifecycle authority; this was counterevidence to FE-A3 and the frontend portion of PRG-A4, not only the FE-I13 presentation repair. | Resolved: requested-install polling starts only after successful backend admission; already-existing release discovery remains immediate. Root re-accepted FE-A3/M0 and M3-S3a after the corrected public-hook boundary passed. | Red observed one early progress call while admission was deferred; green observed zero early calls then exactly one current-lifecycle call after success, with all prior serialization/supersession cases green. |
| `PRG-I13` | High | Platform M4 red evidence showed Bash retains wrapper-specific release policy, unknown `Plan9` does not reject, and a resistant real child exceeded the 200 ms graceful plus 100 ms force bound by about 1.23 seconds. Pre-acceptance review then found duplicated identical POSIX tree-kill mechanisms and a Windows `taskkill.exe` helper whose Promise was not joined to the outer terminal result. | Launcher action/platform/process-tree ownership; blocked DRBT-A6 and the launcher portion of PRG-A4. | Resolved at local source revision `a89b6c12`: one POSIX Seam owns Linux/macOS mechanics, the active Adapter Promise is joined before public settlement, and the Windows helper has a bounded observed lifecycle. Root accepted the corrected source after 41 launcher tests; no gate/schedule or root-package change occurred. | Passed locally: composed child-close-before-helper, Windows success/nonzero/timeout, broad-margin incomplete cleanup with fixture cleanup, wrapper parity, unsupported platform, and real Linux descendant removal. DRBT-A6 remains pending only for unavailable Windows/macOS required-real runs and final focused reconciliation. |
| `PRG-I14` | High | Rust M2F's producer-owned `ConversionProgressOutcome` preserves the core contract's camelCase inner fields, while the handwritten frontend `api-conversion.ts` claims snake_case progress fields. | Cross-process contract drift; does not invalidate the closed Rust producer slice but blocks PRG-A2 consumer acceptance. | Preserve Rust as semantic owner. Platform M1 must generate/decode its exact shape after zero-Legacy, and frontend M4 must delete/migrate the handwritten mismatch rather than making another local schema copy. | Malformed/mismatched conversion payload corpus fails explicitly through the generated Electron/preload Adapter and renderer consumer tests. |
| `PRG-I15` | High | Rust M2H review found the core interrupted-download scan mapped a `spawn_blocking` join failure to an empty list, while an intentionally disabled or failed-to-initialize HF client mapped status/mutation/list calls to not-found, false-without-reason, or successful empty data. | Producer truth and lifecycle composition; invalidated the affected M2H closed-outcome claim and would have made platform M1 generate unavailable/failure states as valid missing/empty results. | Resolved: the canonical core/public/desktop seams return disabled HF as `Err`, preserve task-join failure, and keep legitimate missing/false/empty results distinct. The transitional UniFFI caller only projects `Result` pending its accepted removal; no manifest/shared expansion occurred. | Passed: direct disabled-HF and deterministic join-failure tests, real desktop valid-missing/empty cases, real local-IPC method rejection, full core/RPC/adapter suites, default/no-default Clippy/check, format, and diff checks. This seam remains valid, but PRG-I19 separately reopens the recovery mutation commands, so aggregate M2H/count acceptance is suspended. |
| `PRG-I16` | Medium | The post-M3 full frontend suite initially passed 470/471: unchanged `ModelMetadataModal.test.tsx` still queried deleted accessible backdrop label `Close metadata modal` and emitted `act` warnings from unawaited initial loads. | Focused evidence reconciliation; frontend owns the stale caller test, and the failure blocked M3-S3b/full-tranche acceptance without invalidating accepted ModalDialog behavior. | Resolved test-only: the caller awaits initial metadata, drives the aria-hidden backdrop's actual `mousedown` Interface, and sends Escape to `document`; no product source or obsolete accessible backdrop was restored. | Passed: focused modal/Popover tests, full lint/types/builds, and the expanded full frontend suite at 109 files/473 tests without the prior warnings. |
| `PRG-I17` | High | Platform M3 found that the reachable GUI-managed Torch path installed upstream source while its launcher expected Pumas `serve.py`; the manifest also advertised Python 3.10 despite the repository pinning 3.12.3. The embedded sidecar, upstream wheel resolver, and managed runtime path have since been implemented and exercised through RPC, including real 2.10 CUDA/Tuldok inference. | Cross-plan deployment authority; Torch runtime installation/process ownership stays in `pumas-app-manager`, sidecar execution stays in `torch-server`, and desktop/API projection consumes the RPC contract. | **Resolved within the repository-owner-authorized scope:** discover stable upstream `vMAJOR.MINOR.PATCH` tags independently of Pumas qualification, subject to a 20-page / 120-second complete-listing budget that fails without partial success. Offer CPU/CUDA/ROCm binary-wheel installation only when the full official hash-locked dependencies resolve for Linux x86_64 and an installed CPython 3.10–3.13. Do not provision Python, compile Torch from source, or claim XPU/other operating systems. Keep install, selection, default, start, and stop explicit. | Passed U1–U4: live discovery returned 63 stable tags; dynamic `v2.9.0+cpu` / CPython 3.12 preview resolved 25 hashed wheels, installed, passed identity/core probe, was selected, passed managed sidecar health/protocol 3, and stopped by owned generation. U6 now discovers release-specific official wheels, filters channels against detected display vendors, recommends only when scan and driver checks support it, and defaults to a Pumas-owned FLUX.2 profile with build/Python/profile choices in Advanced. The live `v2.14.0` scan returned no `cu134`; it does not establish a completed v2.14 dependency preview, install, or image generation. Cancellation/state-preservation and typed-preview rejection tests passed; the composed desktop projection test passed. See the [accepted plan](../torch-upstream-version-management/plan.md), [current runtime inventory](../torch-diffusion-serving/reports/upstream-version-manager-progress.md), and [retained acceptance summary and artifact manifest](../torch-upstream-version-management/reports/v2.9.0-cpu-rpc-acceptance.json). This is runtime-manager acceptance via RPC plus composed desktop controls, not a real interactive desktop run or an all-release image claim. The actual Tuldok image result remains the exact Torch 2.10 / CUDA 13.0 / Python 3.12 / FLUX.2 tuple. |
| `PRG-I18` | High | Rust M2I review found four corrected producer counterexample families, then downstream consumer review found a fifth: a complete Q5 row still carried subordinate selected-Q4 recovery identity, and the current renderer prioritizes that identity when matching activity, falsely attaching Q4 state to the ready Q5 row. The earlier families were whole-catalog rejection of valid mixed state, bidirectional hierarchy dirtiness, host-dependent recovery grammar, and bounded run-ID collision. | Rust core/RPC catalog contract and program sequence; the consumer contradiction reopens M2I and blocks its count increase, M2J, RUST-A2, platform M1 generation, and frontend M4 consumption even though the corrected hierarchy/list/grammar/bounds subclaims remain valid. | Active corrective gate: `Complete` cannot carry subordinate recovery identity; only a truly partial row may expose a recovery action. Preserve authoritative ready classification without a renderer-unneeded mixed state, and keep the earlier corrected producer evidence as partial subclaims. | Cross-owner Q5-complete/Q4-part projection must serialize complete with no recovery identity, then the actual renderer matching/row-state corpus must keep Q5 ready and must not attach Q4 activity. Re-run both feature modes and independent review before M2I counts advance or M2J starts. |
| `PRG-I19` | Critical | The original M2H recovery request accepted caller-owned repository/path authority. Corrected Slice A closes the durable publication/store prerequisite, and fourth-freeze Slice B now closes the deep task/blocking owner after three rejected boundaries exposed reservation, drain, projection, cancellation, callback, and filesystem-custody defects. C1 and C2 are internally verified, and C3's narrow ordinary admission/resume/restore checkpoint passed review and targeted evidence. The interrupted-response/fresh-owner-reopen path is verified; full C3 is active after PRG-I24's migration scope approval; hard process-crash recovery, stable runtime queue identity, comprehensive owned restore, stalled pause, relocation, and guard-free effects remain incomplete. Builder callback wrappers still spawn the real importer mutation and return before lifecycle observation; C4 owns that repair. Live desktop consumers still send the removed ambient shape. | Rust M2H mutation/filesystem/lifecycle authority plus the M2I/frontend handoff. The unresolved ordinary lifecycle can publish false active state, strand Pausing, signal or call user code under a guard, surrender shared marker/part authority while a state remains resumable, lose FIFO/crash truth, let importer metadata mutate after cancel/release, or regain ambient/path-string authority. M2I recovery/count acceptance stays reopened and M2J, RUST-A2, downstream generation/consumption, and incompatible standalone producer integration remain blocked. | The aggregate boundary remains decomposed into five serialized slices. Slices A/B and C1/C2 are internally accepted; C3 has only its narrow admission checkpoint verified. The focused Rust plan owns current evidence and remaining integration limits. Slice C uses the focused Rust plan's exact source write set, including its bounded shared finalization seam. It makes admission durably ordered before public success/effects; derives destination identity and effects from one held configured root plus validated relative target; exclusively persists ordinary, queue, and Pending/Verified quarantine truth with independent sticky provenance; retains Paused/recoverable Error or Pending authority; releases only verified published terminal state by exact generation with panic rescue; binds pause/resume/relocation to the owner; linearizes guard-free snapshots; owns the actual Aux/Completion importer futures under destination custody; and keeps public synchronous callbacks notification-only. No new public constructor/wire outcome is admitted. Preserve all A/B publication, custody, cleanup, failure-provenance, and projection guarantees. One active Pumas runtime per root remains the task-admission precondition. General client Drop remains M4/RUST-A6; public core/UniFFI ambient routes are transitional only. | Slice A passed both reviews after 18 atomic and 22 store tests per mode. Slice B passed both reviews and root reproduction at exact fourth-freeze hashes with lifecycle 18/18 and download 85/85 per mode, both Clippy modes, format/diff, and current plan gates. Slice C requires deterministic durable-admission/FIFO crash restore and malformed-v3 graph validation, quarantine begin/verify/remove ambiguity, alias/missing/replacement identity, atomic-marker ambiguity, production importer mutation/cancel/successor ordering, exact/partial/auxiliary/recovery-domain contention, pause/final-cleanup winners, dormant resume/cancel/relocation, terminal panic rescue, and reentrant guard/signal oracles; all A/B regressions and focused dual-mode gates must pass before its exact-hash freeze and reviews. Slices D/E then prove domain and closed/full claims. Coherent verified incremental commits follow the root commit decision; compatible reachable contracts are required, and full C3/consumer/release acceptance remains open. |
| `PRG-I20` | High | Cross-owner review of the frozen Electron launcher-root recovery producer found that every resolved authority projected to generic `ready`, so a valid environment/argument authority incorrectly enabled a persisted selection that the same explicit authority would outrank on restart. The native chooser also had no single attempt owner: dialog rejection was not projected through the closed result, concurrent invocations could race, and attempts remained available after `restarting`, `replacement-visibility-unknown`, or `published-durability-unavailable`. | Platform M2 startup/selection lifecycle and the serialized frontend recovery handoff. The producer could not define an honest user decision or integrate with its renderer consumer while selection policy and attempt ownership were ambiguous. | Resolved at the corrected producer source boundary: path-free ready state carries `select-library|correct-launch-input`; only supersedable authority offers the chooser. One main-process handler owns dialog, validation, persistence, and synchronous relaunch request; concurrent callers share its Promise; unchanged outcomes explicitly retry; ambiguous, published, and restarting outcomes lock/replay; dialog and relaunch rejection have closed typed outcomes. | Passed locally: 18 focused recovery/preload checks, full Electron build and seven test files, lint, TypeScript validation, plan structure, and diff checks. Program independently reran the full Electron tests/lint/validation. These lifecycle subclaims remain valid, but PRG-I21 separately reopens the uncommitted producer/consumer tranche because the compiled sandboxed preload cannot load its decoder module. |
| `PRG-I21` | Critical | The first composed Electron run exposed a missing compiled preload decoder and later reviews found no-bridge fallback, zero-exit fatal, raw-error, and first-frame ordering defects. Provider-only state timing, layout effect, two animation frames, and untagged/count-based presentation callbacks all failed construction or real compositor review. The opaque marker-present then marker-free challenge is correctness-green, but the representative delayed-default path took about 1.07 seconds from semantic commit to show. A terminal-state bootstrap then made Checking unconstructible and acknowledgement preceded `ready-to-show`, yet the only delivered pre-show NativeImage was still an uninitialized solid surface. The admitted hidden-capture probe also failed: its first default capture took about 1.30 seconds and returned ordinary unmarked UI. | Platform preload/bootstrap/window ownership and the serialized frontend recovery consumer. This blocks the producer/consumer atomic commit, Platform M2/DRBT-A3 recovery evidence, and the immediate/truthful library workflow; it does not invalidate PRG-I20, the accepted standalone preload-loader subclaim, or the focused-green no-bridge repair. | Both bounded alternatives are rejected and product source is frozen. The causal marker remains the only correctness-green design but is too slow for the normal path. An explicit product decision must either accept and document that roughly one-second reveal delay or authorize a newly bounded presentation-architecture investigation. Immediate reveal without causal first-frame proof is not admissible. No Electron/Frontend source tranche may integrate before that decision and its resulting composed evidence. | If the safe marker is selected, rerun the complete composed matrix and record the delay honestly. If a broader architecture investigation is selected, re-plan its exact Module, Interface, write set, failure/cleanup owner, required-real targets, and stop condition before source mutation. Final evidence still requires immediate/delayed/never-terminal startup, nonzero fatal, preload/no-preload, closed startup/selection values, malformed/extra rejection, and bounded residue. No DOM-only, delayed post-show, elapsed-threshold, or frequency claim substitutes for the first visible framebuffer. |
| `PRG-I22` | High | A transient Coding Standards revision `40d87cf3` exposed Frontend XR-S1's noncanonical `Review pending` milestone and `review-pending` acceptance/lifecycle projections. Standards returned to the execution-start lineage, retired the final Bash plan helper at `1956679f`, and now resolve to committed HEAD `ef400727`; typed `plan_contract` enforcement remains owned by the registered planning suite and its Python check. | Program-wide standards replacement review, checker selection, and plan lifecycle projection. Root owns the baseline/acceptance hold; each focused owner owns only its plan correction. This is an enforcement correction, not authority to reopen accepted source claims. | Resolved: Frontend uses canonical current states throughout its plan, ledger, issues, and evidence report, with zero current `review-pending` projections. Exact current-lineage review found no changed canonical Router, Core, workflow, topic, profile, language, or template file. The current typed owner accepts all five Pumas plan contents, and the registered planning suite accepts its complete owner/fixture contract. Retired helper results remain historical only. | Passed at `ef400727e2d81a467af64b95f64e7c631096faee`: direct current `validate_plan` use returns `valid` for each of the five Pumas plans; registered `planning-consolidation` passes 26/26 checks; root diff checks pass. The `9790e576..ef400727` delta completes the verification-engine Bash migration and changes verifier/evaluation implementation, registries, generated inventories, and Standards-repository plans, but no canonical normative file. The exact `9790e576` and earlier results remain historical evidence, not current gate authority. Standards `--complete` remains repository-internal and is not substituted for the external plan-content claim. |
| `PRG-I23` | Critical | Read-only post-Rust review found the candidate closed camelCase Rust catalog and `{modelId,recoveryToken}` action are not consumed by Electron/frontend: preload forwards raw catalog values into legacy open `ModelRecord.metadata`, recovery still sends repository/destination authority, optional outcomes are treated as contract, and v1 local storage is unscoped and persists recovery/activity/action data. It can display prior-root rows or retain stale capability while fresh projection throws. | Internal-coordinated generated producer/platform/frontend replacement. This blocks M2I/M2J consumer integration, Platform M1/M2, Frontend M4, PRG-I21 composition, and incompatible standalone Rust producer integration; it does not reject the frozen Rust semantic owner or authorize mixed source edits. | Active re-plan only: invalidate/evict v1; define a strict root-scoped display-only v2 cache with no model path/token/activity/action state; generate renderer-consumable types from the canonical Rust producer; add terminal synchronous bootstrap with opaque stable scope identity; centralize closed catalog projection; expose fresh-only model-ID recovery; preserve unassociated downloads as orphan rows without heuristics. Execute serialized M4-S0 inventory, S1 projection, S2 scoped lifecycle, S3 row/action consumers, then separate link/picker/generated-deletion dispositions. | Product first chooses the narrowed PRG-I21 representative claim, workload, and budget. Then Rust freezes M2I B-E/M2J, Platform generates/decodes both consumers and supplies root scope/bootstrap, Frontend completes the admitted slices, and real composed evidence covers every closed/malformed variant, Q5/Q4 identity, v2 scope/secret stripping, cache/fresh/degraded lifecycle, action single-flight/outcomes, a11y, and both renderer modes. No consumer source is admitted. Coherent verified incremental commits follow the root commit decision; incompatible producer-only integration remains held. |
