# ComfyUI Setup Launcher - Python Dependencies
# Target: Python 3.12+

# Desktop GUI Framework
pywebview>=5.0,<6.0

# Application Packaging
pyinstaller>=6.0,<7.0

# Cross-platform file opener
click>=8.1,<9.0

# Process introspection for download progress
psutil>=5.9,<6.0

# Process Title Management (for ComfyUI patch)
setproctitle>=1.3,<2.0

# Dependency metadata parsing
packaging>=23.0,<25.0

# Icon generation for version-specific shortcuts
Pillow>=10.0,<11.0

# Caching utilities for request deduplication
cachetools>=5.3,<6.0

# Model library support
huggingface_hub>=0.23,<0.25
pydantic>=2.7,<3.0
tenacity>=8.2,<9.0
blake3>=1.0,<2.0

# HTTP/2 networking (Phase 5 - Networking & Caching)
httpx[http2]>=0.27,<1.0

# Note: PyGObject (GTK bindings) is used from system installation
# Install via: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# This is pre-installed on most Debian-based distros
