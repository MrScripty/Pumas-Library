{
  "name": "pumas-library-electron",
  "version": "0.2.0",
  "description": "Electron wrapper for Pumas Library",
  "main": "dist/main.js",
  "author": "Pumas Library Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MrScripty/Linux-ComfyUI-Launcher.git"
  },
  "scripts": {
    "validate": "tsc --noEmit",
    "prebuild": "npm run validate",
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron . --dev",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "package": "npm run build && electron-builder",
    "package:linux": "npm run build && electron-builder --linux",
    "package:win": "cd ../frontend && PUMAS_MULTI_APP=false npm run build && cd ../electron && npm run build && electron-builder --win",
    "package:mac": "cd ../frontend && PUMAS_MULTI_APP=false npm run build && cd ../electron && npm run build && electron-builder --mac"
  },
  "dependencies": {
    "electron-log": "^5.0.0"
  },
  "devDependencies": {
    "electron": "^38.0.0",
    "electron-builder": "^25.0.0"
  },
  "build": {
    "appId": "com.pumas.library",
    "productName": "Pumas Library",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../frontend/dist",
        "to": "frontend",
        "filter": ["**/*"]
      },
      {
        "from": "resources/bin",
        "to": ".",
        "filter": ["pumas-rpc", "pumas-rpc.exe"]
      }
    ],
    "linux": {
      "target": [
        {
          "target": "AppImage",
          "arch": ["x64"]
        },
        {
          "target": "deb",
          "arch": ["x64"]
        }
      ],
      "category": "Development",
      "maintainer": "Pumas Library Team"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "signtoolOptions": {
        "publisherName": "Pumas Library Team"
      }
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Pumas Library"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": ["arm64"]
        }
      ],
      "category": "public.app-category.developer-tools"
    }
  }
}
